{
  "name": "docksbruxsel",
  "description": "ElectronApp",
  "version": "1.0.0",
  "build": {
    "appId": "docks.bruxsel"
  },
  "scripts": {
    "pack": "build --dir",
    "dist": "build"
  },
  "postinstall": "install-app-deps",
  "devDependencies": {
    "electron": "^1.4.3",
    "electron-builder": "^7.14.1"
  }
}
